# 项目协作指令

## 沟通

- 所有用户可见内容使用中文。
- 先说结果和当前状态，技术细节只保留解决问题所需的部分。
- 进度更新只在完成里程碑、出现真实阻塞或需要用户决定时发送。
- 不为显而易见的小步骤反复解释流程。

## 执行原则

- 默认直接实施用户已经明确的要求；缺失的项目文件和普通依赖直接补齐。
- 需要管理员权限、系统级安装或其他平台审批时，按工具规则申请批准。
- 优先完成可用的 MVP，遵循 YAGNI；不主动增加企业级、品牌化或“更专业”的非必要工作。
- 图标、视觉润色和多安装格式不是当前优先级，除非用户明确要求。
- Windows MVP 默认只维护和发布 NSIS `.exe` 安装包。

## 小修改

对范围明确的 bug、小功能或文档修改，使用以下流程：

1. 确认问题或写一个能复现问题的相关测试。
2. 直接修改。
3. 运行相关测试。
4. 测试通过后提交。

小修改不要：

- 单独创建设计规格或详细实施计划；
- 为每个小问题启动独立复核；
- 每改一处就运行完整测试套件；
- 重复询问已经由用户决定的实现方向。

## 大修改

- 只有跨多个独立子系统、存在重要产品取舍、数据迁移、安全风险或用户明确要求时，才先写设计和计划。
- 大修改开发期间运行相关测试；全部完成后只运行一次完整回归。
- 独立代码复核只用于大功能、高风险变更或用户明确要求，不作为每次提交的固定步骤。

## 测试、提交与推送

- 测试强度与改动风险成比例。
- 改动范围决定验证范围：小模块只测试该模块，跨模块接口只测试接口两端。
- 不得因为一个局部问题自动扩大为全项目拉通、重新打包、重新安装或完整回归。
- 纯文档修改不运行代码测试。
- 开发阶段优先运行相关测试；准备交付前统一运行一次完整回归，不重复运行无新增证据的测试。
- 如果用户明确取消完整回归，相关模块测试通过后即可提交和推送。
- 相关测试通过即可提交当前修改。
- 任务完成且最终回归通过后，推送当前功能分支。
- 除非用户明确要求，不创建 Pull Request，不合并主分支，不删除工作树。

## 自主性

- 能从项目和上下文安全推断的内容直接决定，不把小选择反复交给用户。
- 只有会显著改变产品行为、扩大范围、产生外部影响或存在不可逆风险时才询问用户。
- 用户要求“直接实施”“缺失的补齐”或“下一步”时，继续推进到可验证结果，不在流程节点停住等待形式确认。
